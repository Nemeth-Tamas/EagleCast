[package]
name = "eaglecast"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.36"
reqwest = { version = "0.12", default-features = false, features = [
    "blocking",
    "json",
] }
serde = { version = "1.0", features = ["derive"] }
